# MCP Mastyf AI — Environment Variables
# Copy this file to .env and fill in values as needed.

# NVD API key (optional, for NVD CVE lookups)
# Get a free key at: https://nvd.nist.gov/developers/request-an-api-key
NVD_API_KEY=

# OpenAI API key (for tiktoken token counting — not strictly required if using local models)
OPENAI_API_KEY=

# Database path (defaults to ~/.mastyf-ai/history.db)
MASTYF_AI_DB_PATH=
# Audit retention (days, default 30; SOX/PCI often 90–365)
# MASTYF_AI_RETENTION_DAYS=30
# Field encryption at rest (requires MASTYF_AI_DB_ENCRYPTION_KEY)
# MASTYF_AI_DB_ENCRYPTION_KEY=
# MASTYF_AI_DB_ENCRYPT_AUDIT_ARGS=true
# JWKS refresh for OAuth JWT validation (default 5 minutes)
# MASTYF_AI_JWKS_REFRESH_MS=300000
# Payload limits (bytes)
# MASTYF_AI_MAX_PAYLOAD_BYTES=10485760
# MASTYF_AI_MAX_ARGUMENT_BYTES=10485760
# MASTYF_AI_SCAN_TOOL_TIMEOUT_MS=15000
# MASTYF_AI_SCAN_TOOL_TIMEOUT_MAX_MS=15000
# MASTYF_AI_SEMANTIC_STRICT=true
# MASTYF_AI_MAX_EXPANDED_PAYLOAD_BYTES=52428800
# MASTYF_AI_STRICT_ALLOWLIST_RBAC=true
# MASTYF_AI_HEALTH_PROBE_INTERVAL_MS=0
# MASTYF_AI_SHUTDOWN_GRACE_MS=30000
# SIEM exporters (splunk, datadog, webhook, etc.)
# MASTYF_AI_SIEM_ENABLED=true
# MASTYF_AI_SIEM_EXPORTERS=splunk,webhook
# MASTYF_AI_SIEM_SPLUNK_HEC_URL=
# MASTYF_AI_SIEM_SPLUNK_HEC_TOKEN=

# Custom pricing overrides (USD per 1M tokens)
# Example: PRICING_OVERRIDES='{"my-model": {"input": 2.0, "output": 6.0}}'
PRICING_OVERRIDES=

# Log level: DEBUG, INFO, WARN, ERROR (default: INFO)
LOG_LEVEL=INFO

# ── OpenTelemetry (distributed tracing) ───────────────────────
# OTLP HTTP endpoint (omit to disable tracing)
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
# OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=http://localhost:4318/v1/traces
# OTEL_SERVICE_NAME=mastyf-ai
# Set OTEL_ENABLED=false to disable even when endpoint is set
# OTEL_ENABLED=true

# ── Dashboard & live TUI ─────────────────────────────────────
# Enable the dashboard REST API (HTML at / always lists API + TUI docs)
DASHBOARD_ENABLED=true
# WebSocket push for TUI (default true when proxy starts)
MASTYF_AI_WS_ENABLED=true
MASTYF_AI_WS_PUSH_INTERVAL_MS=5000
# Dashboard server port (default: 4000)
DASHBOARD_PORT=4000
# TUI: merge metrics from dashboard (optional)
MASTYF_AI_DASHBOARD_URL=http://127.0.0.1:4000

# ── AI learning (enabled by default; set MASTYF_AI_AI_ENABLED=false to disable) ──
MASTYF_AI_AI_ENABLED=true
MASTYF_AI_AI_AUTO_APPLY=false
MASTYF_AI_AI_USE_DB_SNAPSHOTS=true
# MASTYF_AI_AI_DISABLE_THREAT_POLL=false
# MASTYF_AI_AI_THREAT_POLL_MS=1800000
# MASTYF_AI_SWARM_USE_LEGACY_ARTIFACTS=false
# MASTYF_AI_DASHBOARD_STRICT_LIVE=true  # default: hide stale/committed swarm artifacts until a job runs this session
# Mastyf AI Autopilot (or: mastyf-ai autopilot init && autopilot start)
# MASTYF_AI_AUTOPILOT=true
# MASTYF_AI_THREAT_RESEARCH_AUTO=true
# MASTYF_AI_THREAT_DISCOVERY_AUTOSTART=true
# MASTYF_AI_REPORT_SCHEDULE=daily
# MASTYF_AI_REPORT_CRON_HOUR=6
# MASTYF_AI_AUTOPILOT_CORPUS_GATE=true
# MASTYF_AI_LLM_ENABLED=true
# MASTYF_AI_LLM_MODEL=qwen3:8b
# OLLAMA_BASE_URL=http://localhost:11434
# MASTYF_AI_SEMANTIC_ASYNC=true  # on by default when LLM enabled; post-hoc semantic audit (Pro)
# MASTYF_AI_UPSTREAM_TIMEOUT_MS=30000
# MASTYF_AI_REGEX_EVAL_TIMEOUT_MS=50
# MASTYF_AI_REGEX_USE_WORKER=true  # worker-thread regex eval (default on in production)
# MASTYF_AI_POLICY_REJECT_UNSAFE_REGEX=true  # fail policy load on ReDoS-risk patterns (default true in production)
# MASTYF_AI_SEMANTIC_LLM_TIMEOUT_MS=30000  # alias for MASTYF_AI_LLM_TIMEOUT_MS in @mastyf-ai/core
# MASTYF_AI_SEMANTIC_LLM_MAX_PER_MIN=10
# MASTYF_AI_SEMANTIC_LLM_MAX_USD_PER_MIN=0.03  # optional; default = max_per_min × estimated cost
# MASTYF_AI_SEMANTIC_CIRCUIT_THRESHOLD=5
# MASTYF_AI_SEMANTIC_CIRCUIT_RESET_MS=60000
# MASTYF_AI_TENANT_AUDIT_PATHS=true
# MASTYF_AI_PG_RLS_ENABLED=false
# MASTYF_AI_OPEN_CORE=true  # deprecated in v3.0 — Pro gates always apply
# MASTYF_AI_DEV_UNLOCK_ALL=true  # with NODE_ENV=development only (maintainers)
# MASTYF_AI_CI_BYPASS_LICENSE=true  # CI workflows only — not for production
# MASTYF_AI_LICENSE_KEY=  # Optional: cloud org API key for control-plane linking
# MASTYF_AI_CONTROL_PLANE_URL=https://your-cloud.vercel.app
# MASTYF_AI_JWT_MAX_LIFETIME_SEC=86400
# MASTYF_AI_JWT_STRICT_LIFETIME=false  # fail startup when max lifetime > 86400
# MASTYF_AI_SEMANTIC_SYNC_RESPONSE=false  # production defaults ON unless explicitly false
# ALERT_SLACK_WEBHOOK=  # Slack incoming webhook (canonical app alert destination)
# ALERT_PAGERDUTY_KEY=  # PagerDuty Events API v2 routing key
# ALERT_WEBHOOK_URL=  # Legacy fallback for Slack/Discord (used when ALERT_SLACK_WEBHOOK unset)
# ALERT_MIN_SEVERITY=warning  # Minimum severity for webhook delivery
# MASTYF_AI_ALERTING_REQUIRED=false  # enterprise Helm sets true — fail startup without alert destinations
# MASTYF_AI_CLUSTER_ALERTING_ONLY=false  # skip app webhook requirement when Alertmanager-only
# SWARM_ALERT_WEBHOOK_URL=  # security swarm failure webhook (falls back to ALERT_WEBHOOK_URL)
# SWARM_ARCHIVE_RETENTION_DAYS=30
MASTYF_AI_AI_MIN_BASELINE_SAMPLES=3
MASTYF_AI_TUI_SKIP_LEARNING=false
MASTYF_AI_AI_SKIP_INITIAL_CYCLE=false
# Run AI learning on scan/audit/health/report (default: proxy/TUI only)
MASTYF_AI_AI_ON_CLI=false
MASTYF_AI_SKIP_PREFLIGHT_SCAN=false
# Opt-in: block tools/call when upstream scan has CVEs at/above threshold (default CRITICAL)
MASTYF_AI_BLOCK_ON_CVE=false
# MASTYF_AI_CVE_BLOCK_SEVERITY=CRITICAL

# ── Industry-standard roadmap (A1–C5, B1–B3) ─────────────────
# Cross-MCP fleet chain blocking (default confidence 0.65)
# MASTYF_AI_FLEET_CHAIN_BLOCK_CONFIDENCE=0.65
# MASTYF_AI_FLEET_CHAIN_REDIS=true
# MASTYF_AI_FLEET_REGION=us-east-1
# MASTYF_AI_FLEET_PEER_REGIONS=eu-west-1,ap-southeast-1
# Optional ONNX graph classifier for fleet chain scoring (A1)
# MASTYF_AI_FLEET_GRAPH_ONNX_MODEL=/path/to/graph-model.onnx
# MASTYF_AI_FLEET_GRAPH_ONNX=false
# Ecosystem observatory cloud relay (B2); use stub for local dev without relay
# MASTYF_AI_OBSERVATORY_RELAY_URL=https://your-cloud.example/api/v1
# MASTYF_AI_OBSERVATORY_RELAY_API_KEY=
# MASTYF_AI_OBSERVATORY_STUB=true
# Federated threat learning (B3)
# MASTYF_AI_FEDERATED_LEARNING=true
# MASTYF_AI_FEDERATED_ONNX_MODEL=/path/to/model.onnx
# MASTYF_AI_FEDERATED_MPC=true
# MASTYF_AI_FEDERATED_MPC_SECRET=
# Behavioral biometrics warmup before enforcement (A3)
# MASTYF_AI_BIOMETRICS_MIN_SAMPLES=10
# Zero-trust geo allowlist (C3)
# MASTYF_AI_ZERO_TRUST_ALLOWED_REGIONS=US,CA,GB
# SPIFFE workload identity socket (C3)
# MASTYF_AI_SPIFFE_SOCKET_PATH=/run/spire/sockets/agent.sock

# ── Dashboard Authentication ───────────────────────────────────
# Auth on by default when dashboard is enabled. Local dev only:
# DASHBOARD_AUTH_DISABLED=true
DASHBOARD_AUTH_ENABLED=true
# API Key for simple authentication (use /?api_key=<key> or Authorization: Bearer <key>)
DASHBOARD_API_KEY=
# JWT HMAC secret for session-based authentication (generate: openssl rand -hex 32)
DASHBOARD_JWT_SECRET=
# Credentials for username/password login (requires DASHBOARD_JWT_SECRET)
DASHBOARD_USERNAME=admin
DASHBOARD_PASSWORD=
# Comma-separated allowed origins for CSRF validation
DASHBOARD_ALLOWED_ORIGINS=http://localhost:4000,http://localhost:3000,http://127.0.0.1:4000
# Allow public access to /metrics without authentication
DASHBOARD_METRICS_PUBLIC=false

# ── Metrics ────────────────────────────────────────────────────
METRICS_ENABLED=true
METRICS_PORT=9090
# Redis URL for distributed session/rate-limit store
REDIS_URL=

# ── Policy Audit ───────────────────────────────────────────────
# Enable policy change audit logging (JSONL format)
POLICY_AUDIT_ENABLED=true
POLICY_AUDIT_LOG=./policy-audit.jsonl

# ── Payload Normalization (v1.2+) ──────────────────────────────
# Max decode depth for URL/hex/entity normalization (default: 5)
NORMALIZER_MAX_DEPTH=5
# Max input size for normalization in bytes (default: 1000000)
NORMALIZER_MAX_LENGTH=1000000