{{- if and .Values.gateway.enabled .Values.gateway.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "mastyff-ai.fullname" . }}-gateway
  labels:
    {{- include "mastyff-ai.labels" . | nindent 4 }}
  {{- with .Values.gateway.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.gateway.ingress.tls }}
  tls:
    - hosts:
        - {{ .Values.gateway.ingress.host | quote }}
      secretName: {{ .Values.gateway.ingress.tlsSecret | default (printf "%s-gateway-tls" (include "mastyff-ai.fullname" .)) }}
  {{- end }}
  rules:
    - host: {{ .Values.gateway.ingress.host | quote }}
      http:
        paths:
          - path: /sse
            pathType: Prefix
            backend:
              service:
                name: {{ include "mastyff-ai.fullname" . }}-gateway
                port:
                  number: {{ .Values.gateway.service.port }}
          - path: /message
            pathType: Prefix
            backend:
              service:
                name: {{ include "mastyff-ai.fullname" . }}-gateway
                port:
                  number: {{ .Values.gateway.service.port }}
{{- end }}
