{
  "name": "@mastyf-ai/mcp-server",
  "version": "4.1.7",
  "description": "Standalone MCP scan server and HTTP proxy helpers for the Mastyf AI monorepo",
  "type": "module",
  "main": "./dist/index.js",
  "exports": {
    ".": "./dist/index.js",
    "./http-proxy": {
      "import": "./dist/http-proxy.js",
      "types": "./dist/http-proxy.d.ts"
    },
    "./http-proxy-utils": {
      "import": "./dist/http-proxy-utils.js",
      "types": "./dist/http-proxy-utils.d.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "tsc && (npm pack --dry-run 2>&1 | grep -q dist/cli.js || (echo 'ERROR: dist/cli.js missing from tarball' && exit 1)) && (node -e \"var p=require('./default-policy.yaml'); if(!['pass','block','flag'].includes(p.policy.default_action)){console.error('ERROR: invalid default_action: '+p.policy.default_action);process.exit(1)}\" 2>/dev/null || true) && echo '\u2705 prepublish checks passed'"
  },
  "dependencies": {
    "@mastyf-ai/core": "workspace:^4.1.7",
    "@modelcontextprotocol/sdk": "^1.25.2"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^3.2.4",
    "@types/node": "^20.0.0"
  },
  "license": "MIT",
  "files": [
    "dist",
    "assets",
    "README.md",
    "LICENSE",
    "default-policy.yaml"
  ],
  "readme": "README.md"
}
